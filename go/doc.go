// Package petstoreserver contains generated transport code.
// NOTE: Treat this package as transport-only; wire dependencies through the composition roots in cmd/,
// and avoid importing it from other modules to keep the hexagonal boundary intact.
package petstoreserver
